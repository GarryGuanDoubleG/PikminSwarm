using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class MeshTools {

	public static bool ContainsPoint(Mesh mesh, Vector3 p)
    {
        int vCount = mesh.vertices.Length;
        return false;
    }
}
